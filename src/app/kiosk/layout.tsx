"use client";

/**
 * Kiosk layout — stores self-hydrate via hydrateFromDb() when accessed.
 */
export default function KioskLayout({ children }: { children: React.ReactNode }) {
    return <>{children}</>;
}
